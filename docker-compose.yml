services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./secrets:/app/secrets:ro
  worker:
    build: .
    command: ["python", "-m", "backend.app.worker"]
    env_file:
      - .env
    volumes:
      - ./secrets:/app/secrets:ro
